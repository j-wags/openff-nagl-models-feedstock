{% set name = "openff-nagl-models" %}
{% set version = "2025.09.0" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 9942ed31ae05685ed6eda998d5a05e156f680f0d494452e60917a46a31093f20

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  noarch: python

requirements:
  host:
    - python {{ python_min }}
    - pip
    - versioningit
    - setuptools
  run:
    - python >={{ python_min }}
    - platformdirs
    - importlib_resources
    - packaging
  run_constrained:
    - openff-nagl >=0.4.0

test:
  imports:
    - openff.nagl_models
  requires:
    - pip

about:
  home: https://github.com/openforcefield/openff-nagl-models
  summary: 'NAGL models released by the Open Force Field Initiative'
  description: |
    This package contains NAGL models released by the Open Force Field Initiative.
    They are intended to be used by OpenFF NAGL.
  license: CC-BY-4.0
  license_family: CC
  license_file: LICENSE
  doc_url: https://github.com/openforcefield/openff-nagl-models
  dev_url: https://github.com/openforcefield/openff-nagl-models

extra:
  recipe-maintainers:
    - lilyminium
    - mattwthompson
    - j-wags
